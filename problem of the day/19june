class Solution {
    public int largestAltitude(int[] gain) {
        int net=0;
        int max=0;
        int sum=0;
        for(int g: gain) {
            sum+=g;
            max=Math.max(max, sum);
        }
        return max;
    }
}
